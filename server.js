const http=require('http');const https=require('https');const ODOO='https://peluchesjdk.odoo.com';http.createServer((req,res)=>{res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','POST,OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');if(req.method==='OPTIONS'){res.writeHead(204);res.end();return;}let b='';req.on('data',c=>b+=c);req.on('end',()=>{const isJ=req.headers['content-type']?.includes('json');const o={hostname:'peluchesjdk.odoo.com',path:req.url,method:'POST',headers:{'Content-Type':isJ?'application/json':'text/xml','Content-Length':Buffer.byteLength(b)}};const p=https.request(o,r=>{res.writeHead(r.statusCode,{'Content-Type':r.headers['content-type']||'text/plain','Access-Control-Allow-Origin':'*'});r.pipe(res);});p.on('error',e=>{res.writeHead(500);res.end(e.message);});p.write(b);p.end();});}).listen(process.env.PORT||3000,()=>console.log('Proxy OK'));
